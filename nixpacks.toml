# Nixpacks configuration for Railway
# Ensures Node.js app builds correctly

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "node server.js"
